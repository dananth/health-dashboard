# Garmin credentials are entered via the Login page — do NOT put them here.

OPENAI_API_KEY=sk-...

# Optional — defaults to sqlite:///./health.db
# On Railway, attach a Volume at /data and set:
# DATABASE_URL=sqlite:////data/health.db
DATABASE_URL=sqlite:///./health.db
